class MessagePost

  def initialize(author, messageId, heading, message, postdate)
    @author = author
    @messageId = messageId
    @heading = heading
    @message = message
    @postDate = postdate
  end


end